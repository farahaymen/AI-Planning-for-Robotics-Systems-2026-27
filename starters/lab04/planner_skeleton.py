def search(blocked, start, goal, algorithm="astar"):
    # Return (path_as_row_column_pairs, number_of_expanded_cells).
    # Implement A* first. Use the reference signature so the replay can run it.
    raise NotImplementedError("Implement frontier, cost, parent and reconstruction")
