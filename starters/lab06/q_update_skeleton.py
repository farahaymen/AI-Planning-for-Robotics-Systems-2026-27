import numpy as np
def q_update(q, state, action, reward, next_state, terminated, alpha=.2, gamma=.95):
    # Compute the target, update ONE table entry in place, return the target.
    raise NotImplementedError("Implement the terminal mask and Q-learning update")
